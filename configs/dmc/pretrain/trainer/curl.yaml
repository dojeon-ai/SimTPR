# defaults
type: 'curl'
num_epochs: 10
t_step: None

obs_shape: None
action_size: None
# aug_types: [random_shift, intensity]
aug_types: [dmc_random_crop]

optimizer:
    type: 'adam'
    lr: 0.0001
    weight_decay: 0.0000001 #1e-6
    betas: [0.9, 0.999]
    eps: 0.00015

# updates
batch_size: 256
update_freq: 2
temperature: 0.1
tau: 0.99

# logging
log_every: 1000
eval_every: 10
save_every: 10
