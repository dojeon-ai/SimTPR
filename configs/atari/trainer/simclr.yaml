# defaults
type: 'simclr'
num_epochs: 25
obs_shape: None
action_size: None
aug_types: [random_shift, intensity]

optimizer:
    type: 'adam'
    lr: 0.0001
    weight_decay: 0.0
    betas: [0.9, 0.999]
    eps: 0.00015

# updates
temperature: 0.1

# logging
log_every: 10
eval_every: 5
